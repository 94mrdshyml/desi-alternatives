# Command Execution & Permission Preference

- User has pre-approved standard development, build, test, and verification commands (`bun test`, `bun run check`, `bun run build`, `bun run lint`, `git status`, `git diff`, etc.).
- Do not hesitate or repeatedly ask for permission for already-approved routine commands.
